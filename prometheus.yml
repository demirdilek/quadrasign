global:
  scrape_interval: 2s # Aggressive interval for immediate feedback during development
  evaluation_interval: 2s

scrape_configs:
  # ONLY monitor the custom Go application
  - job_name: 'api-prober'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['api-prober:8080']